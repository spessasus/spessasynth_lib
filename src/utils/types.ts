export type WaveMetadata = {
    // the song's title.
    title: string;
    // the song's artist.
    artist: string;
    // the song's album.
    album: string;
    // the song's genre.
    genre: string;
};
