export interface WaveMetadata {
    // The song's title.
    title: string;
    // The song's artist.
    artist: string;
    // The song's album.
    album: string;
    // The song's genre.
    genre: string;
}
